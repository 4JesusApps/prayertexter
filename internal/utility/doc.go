/*
Package utility contains general helper functions or implementations that are used other packages in prayertexter. This
package can contain various "random" things that are decided at the time to not be implemented in a dedicated package.
It could be that they are reusable tools, or something that does not quite fit inside any of the other packages. If a
specific implementation gets too large, it should be moved into it's own package.
*/
package utility
